<!DOCTYPE html>
<html>
  <head>
    <title>TestGuru</title>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_include_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>

  <body>
    <div class="site">
      <header class="site-header">
        <h1 class="site-title"><%= link_to 'TestGuru', root_path %></h1>

        <nav class="site-navigation">
        <% if user_signed_in? %>
          <li><%= t('.welcome_text') %><b><%= current_user.name %></b>!</li>
          <% if current_user.is_a?(Admin) %>
            <li><%= link_to t('.admin_link'), admin_root_path %></li>
          <% end %>
          <li><%= link_to t('.logout_link'), destroy_user_session_path, method: :delete %></li>
        <% else %>
          <ul>
            <li><%= link_to t('.register_link'), new_user_registration_path %></li>
            <li><%= link_to t('.login_link'), new_user_session_path %></li>
          </ul>
        <% end %>
        </nav>
      </header>
      <main class="content-wrapper">
        <article class="site-content">
        <%= render 'shared/flash' %>
          <%= yield %>
        </article><!-- .site-content -->
      </main>
      <footer class="site-footer">
        <p><%= render 'shared/footer' %></p>
      </footer>
    </div>
  </body>
</html>
