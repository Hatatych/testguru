<h1>Questions for <%= @test.title %></h1>

<table>
  <tr>
    <th>Body</th>
    <th colspan="2">Actions</th>
  </tr>
  <%= render @questions %>
</table>
<br>
<%= link_to 'New question', new_test_question_path %> | <%= link_to 'Back', test_path(params[:test_id]) %>
<br>
